# Assets

Place your resume avatar here as **`avatar.jpg`** (or update the image path inside the Markdown).

Recommended:

- Square aspect ratio (e.g. 600 x 600 px)
- File size < 200 KB
- JPG or PNG

The provided `avatar.placeholder.svg` is only a stand-in &mdash; replace it before exporting your real PDF.
